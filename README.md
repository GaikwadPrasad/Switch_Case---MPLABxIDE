# Switch Case — MPLAB X IDE Example

This repository contains a simple **MPLAB X IDE** project demonstrating the use of the **C `switch`/`case` control structure** in an embedded application using a Microchip PIC microcontroller.

The project includes:
- MPLAB X project files
- Source code (`main.c`) with a `switch`/`case` statement example
- Build folders (`build/`, `dist/`) generated by the IDE
- `nbproject/` with MPLAB X project configuration
- A `Makefile` for building with the XC compiler

## 📌 Overview

The `switch`/`case` structure is used to perform **multi-way branching** based on a variable’s value.  
It is typically used in embedded systems to handle multiple input conditions or states efficiently, compared to multiple `if`/`else` statements. :contentReference[oaicite:1]{index=1}

Example pattern:

```c
switch (variable) {
    case VALUE1:
        // handle value1
        break;
    case VALUE2:
        // handle value2
        break;
    default:
        // handle unexpected values
        break;
}
